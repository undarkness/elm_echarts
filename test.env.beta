NODE_ENV = 'production'   // 打包项目 所以 NODE_ENV 还是 production
VUE_APP_URL = '/test/'   // 线上测试环境，部署在 test 目录下 
VUE_APP_MODE = 'test'   // 这个变量表示当前是线上测试环境
outputDir = 'beta'      // 将线上测试环境打包后生成的代码放在 beta 文件夹